function App() {
  return (
    <main className="app-shell">
      <section className="hero-card" aria-labelledby="app-title">
        <p className="eyebrow">Desktop app scaffold</p>
        <h1 id="app-title">Skills Sync Manager</h1>
        <p className="subtitle">
          Manage a central skills library and sync selected skills to target directories.
        </p>
      </section>
    </main>
  );
}

export default App;
